"""Shared, import-only test helpers (no pytest fixtures).

Imported as ``tests.helpers.<module>``; the repo root is put on ``sys.path`` by
the root ``conftest.py`` so this resolves under ``--import-mode=importlib``.
"""
